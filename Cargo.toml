[package]
name = "www-redirector"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14.24", features = ["server", "http1", "http2", "runtime", "tcp"] }
tokio = { version = "1.25.0", features = ["rt", "macros", "signal"] }